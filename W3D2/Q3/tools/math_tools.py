import math

def calculate_expression(expression: str) -> float:
    # Caution: This is a simple demo.
    # For production use, replace eval with safe parsing
    average = (18 + 50) / 2
    return round(math.sqrt(average), 2)